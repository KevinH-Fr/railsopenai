<!DOCTYPE html>
<html>
  <head>
    <title>Railsopenai</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <script defer src="https://kit.fontawesome.com/052cfc60bd.js" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container-fluid d-flex vh-100">
      <div class="row align-self-stretch w-100">
        <div class="col-md-2 mx-auto bg-dark text-white">
          <div class="w-100 text-align-center d-flex justifiy-content-center mt-3">
            <%= form_with(model: Chat.new) do |form| %>
              <%= form.submit '+ New Chat', class: 'btn btn-dark border-light' %>
            <% end %>
          </div>
          <div class="mt-3">
            <h4>Today</h4>
            <turbo-frame id="user-chats">
              <%= render partial: 'chats/chat_list', collection: current_user.chats.reverse if current_user &&  current_user.chats %>
            </turbo-frame>
          </div>

        </div>
        <div class="col-10">
          <%= yield %>        
        </div>
      </div>
    </div>
  </body>
</html>
